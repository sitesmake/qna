<% if @answer.errors.present? %>
	<% @answer.errors.full_messages.each do |message| %>
		$('.answer-<%= @answer.id %>-errors').html('<%= j message %>');
	<% end %>
<% else %>
  $('.answer-<%= @answer.id %>').html('<%= j render @answer %>');
  $('.answer-<%= @answer.id %>-errors').html('');
  $('#answer_body').val('');
<% end %>
